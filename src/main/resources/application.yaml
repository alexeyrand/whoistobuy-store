server:
  port: 8081

spring:
  application:
    name: whoistobuy-items

  datasource:
    url: jdbc:postgresql://postgres-database:5430/witb
#    url: jdbc:postgresql://localhost:5432/witb
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres


  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql:
